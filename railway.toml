[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/backend/Dockerfile"

[deploy]
startCommand = "uvicorn career_forge.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"

# Set in Railway dashboard:
# DATABASE_URL, CORS_ORIGINS, OPENAI_API_KEY, LANGSMITH_API_KEY
