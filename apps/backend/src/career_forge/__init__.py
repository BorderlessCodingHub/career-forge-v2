"""Career Forge backend — domain-oriented FastAPI package.

Package name: ``career_forge`` (Python import path under ``apps/backend/src/``).
Run with ``PYTHONPATH=src`` or install via ``pip install -e .`` from ``apps/backend``.
"""

__version__ = "0.1.0"
